version: '3.4'

services:
  backend:
    build: .
    volumes:
      - .:/usr/src/app
    ports:
      - 80:5000
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password